
name: Docker Series Builds

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: docker build wordpress
      run: |
        docker build ./wordpress -t nishit2408/wordpress:1.0.0


